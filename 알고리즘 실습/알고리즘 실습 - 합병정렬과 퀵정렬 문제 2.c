# include <stdio.h>
# include <stdlib.h>

int find_pivot_index(int L, int l, int r){
}

int main(){
	int i, N, *arr;

	scanf("%d", &N);

	arr = (int *)malloc(N * sizeof(int));

	for (i = 0 ; i < N ; i++){
		scanf("%d", arr+i);
	}

	return 0;
}